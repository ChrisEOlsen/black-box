"""Generated by bb-builder: {{ method }} — one custom JSON endpoint.

The route is already registered in api.json and routes_gen.py. Implement the
body below, then write a test for it: generated code carries its own tests, but
hand-written logic does not.

No SQL here — put data access in a model under models/. See CLAUDE.md
§ Critical Constraints.
"""

from __future__ import annotations

from pydantic import BaseModel

from deps import CacheDep, DatabaseDep
from handlers.envelope import Envelope


class {{ pascal_name }}Response(BaseModel):
    """Replace with the real response shape, then update api.json's
    response-schema for this endpoint so clients see it."""

    status: str


def {{ name }}(db: DatabaseDep, cache: CacheDep) -> Envelope[{{ pascal_name }}Response]:
    """{{ method }} — TODO: implement.

    `db` and `cache` arrive wired and unused; a strict type checker will flag
    them until you use them, which is the point. Reach for a model in `models/`
    rather than querying `db` here.

    Raise from handlers.envelope (`not_found()`, `conflict()`,
    `validation_failed({...})`) rather than returning an error shape by hand.
    """
    raise NotImplementedError("{{ name }} is not implemented yet")
